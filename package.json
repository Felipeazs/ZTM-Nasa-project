{
	"name": "nasa-project",
	"version": "1.0.0",
	"description": "ZTM Nasa project",
	"main": "index.js",
	"scripts": {
		"server": "npm start --prefix server",
		"client": "npm start --prefix client",
		"dev": "npm run client & npm run server",
		"deploy": "npm run build --prefix client && npm run start --prefix server",
		"deploy-cluster": "npm run build --prefix client && npm run cluster --prefix server",
		"test": "npm run test --prefix server && npm test --prefix client"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/Felipeazs/ZTM-Nasa-project.git"
	},
	"author": "felipeazs",
	"license": "ISC",
	"bugs": {
		"url": "https://github.com/Felipeazs/ZTM-Nasa-project/issues"
	},
	"homepage": "https://github.com/Felipeazs/ZTM-Nasa-project#readme",
	"dependencies": {
		"axios": "^0.27.2",
		"arwes": "^1.0.0-alpha.5",
		"react": "^17.0.2",
		"react-dom": "^17.0.2",
		"react-router-dom": "^5.3.0",
		"react-scripts": "^5.0.0"
	}
}
